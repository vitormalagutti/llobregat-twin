# .streamlit/secrets.toml.example
# ─────────────────────────────────────────────────────────────────────────────
# TEMPLATE — copy this file to .streamlit/secrets.toml and fill in real values.
# The real secrets.toml is gitignored and must NEVER be committed.
# ─────────────────────────────────────────────────────────────────────────────

# AEMET OpenData API key
# Register free at: https://opendata.aemet.es/centrodedescargas/inicio
AEMET_API_KEY = "your-aemet-api-key-here"

# ACA SDIM API key (if required — check their developer portal)
# ACA_API_KEY = "your-aca-api-key-here"
